<h1>Listing wines</h1>
 
<table>
  <tr>
    <th>Title</th>
    <th>Long description</th>
  </tr>
 <% if @wines.each do |wine| %>
    <tr>
      <td><%= wine.title %></td>
      <td><%= wine.longdesc %></td>
    </tr>
  <% end.empty? %>
    <tr>
      <td>No wines</td>
    </tr>
  <% end %>
</table>